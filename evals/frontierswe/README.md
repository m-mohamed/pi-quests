# FrontierSWE Evals

`frontierswe-sample@v1` is vendored here for fast local verification.

Use `frontierswe@public-v1` with `--repo /path/to/frontier-swe` to discover the full upstream task corpus from an external checkout.
